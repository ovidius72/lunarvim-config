require('navigator').setup();
-- require('navigator').setup({
  -- lsp_installer = true,
  -- lsp = {
  --   tsserver = {
  --     filetypes = {"typescript", "javascript", "typescriptreact"}
  --   }
  -- }
-- });
